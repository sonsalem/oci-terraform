*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform/
# .terraform.lock.hcl is intentionally NOT ignored - it pins the provider
# versions everyone gets, so it belongs in git.
crash.log
*.pem
